Rolling bundle (integration/next → main) — accumulating; awaits the owner's "ship it". Opened fresh after bundle #122 shipped.

- [x] **Caddie now knows the tree lines** (5ade0fd) — *noticeable*. OSM trees (`natural=tree`) + woods (`natural=wood`/`landuse=forest`/`scrub`/closed `tree_row`) now flow through the SAME played-polyline carry+side pipeline as bunkers/water, so "how far to clear the trees" gets a real grounded answer ("trees R 220-300y") instead of nothing. Per-side aggregation with a ≥3-observation coverage guard; trees appended after the bunker/water cap so they can never evict a real hazard; `HAZARD_GROUNDING_RULE` honesty for unmapped trees. No schema/frontend change. Fable-planned + Fable-reviewed (geometry class): reviewer SHIP (24-bearing parity sweep, 300-trial cap-eviction fuzz); QA PASS (348/348 targeted, 1709/1709 broader non-DB, voice 274/274). Honest gap: committed Bethpage fixture carries 0 tree/woods OSM data (real-fixture positive coverage deferred to a follow-up); synthetic geometry + golden evals cover correctness.

- [x] **Tee-time results now show your actual options** (f9953f2 + 9f0577e) — *noticeable*. Fixes the three linked bugs from the owner's 2026-07-09 screenshots (spec `specs/teetime-results-ux-fixes.md`):
  1. **Show real TIME OPTIONS, not the search window.** New `prefs → searching → options → confirmed` phase. `Searching` no longer collapses results to one auto-picked/auto-booked slot; a new calm yardage-book `Options` list groups results per course — real foreUP slots as tappable "6:10 AM · 2 spots · $24" rows (~5/course + "+ N more"); no-online-booking courses in a quieter section framed as the ASK ("call for a time in your … window"), NEVER presenting the search window as a found time (no-fake-data).
  2. **Displayed window == submitted prefs.** Killed the `windows.find(w => w.date === slot.date)` race that showed a deselected default; Options/Confirmed now consume `asks: DispatchedAsk[]`, a 1:1 projection of the queries actually dispatched — the window shown equals what was submitted by construction.
  3. **Honor the selected courses.** Dispatch no longer radius-drops a checked course; a `filterToSelection` (id-or-normalized-name) guard + honest `emptySelectionNote` ensure an UNSELECTED course (the Forest Park bug) can never be substituted — honest-empty when none of the picks has availability. Closed the voice zero-match hole that silently deselected everything. Plus P2 #4: `muniFromAddress` drops "USA"/"United States of America" (no more "· USA" labels).
  - Fable-planned (crux verified in code: the API returns a slot LIST per course and the frontend was collapsing it → all fixes frontend/dispatch-only, zero backend change). **Reviewer SHIP** — three bugs mutation-verified fixed (neutering `filterToSelection` turns 3 selection tests red), no-fake-data + selection guards have real teeth, scope clean. **QA PASS** — lint/tsc/build clean, teetime-unit 205/205 (incl. new `options.test.ts`), full suite 1880/1880, voice smoke 274/274, backend ruff clean, backend tee-time unit 102/102. **Designer APPROVE** — live-screenshotted at 390px; reuses PaperShell/Section/Kicker yardage primitives, honest real-slot vs call-route split, 5-row cap reads clean.
  - Follow-ups (non-blocking): route-entry section header "No online times/Call to book" can contradict a `book_on_site` row's own honest copy in a mixed batch (make it conditional); add distance/city to route rows; harden `filterToSelection` Sets against falsy id/name; sub-44pt tap targets (pre-existing across the tee-time flow).

- [x] **Tee-time prefs/results visual polish** (945de5c) — *noticeable*. The polish half of the owner's 2026-07-09 screenshots (spec `specs/teetime-results-ux-fixes.md` #5 + #4), designer-led. Frontend-only, ZERO backend, ZERO logic change to the selection/options flow above.
  1. **Clipped "WHERE / N SELECTED" header** — real cause traced: in the full-bleed Capacitor/standalone WKWebView (viewport-fit cover + black-translucent status bar) content scrolls UNDER the transparent status bar, so the mid-page "Where" Section collided with the clock. Fix = a fixed, pointer-transparent status-bar scrim in `PaperShell` (height `env(safe-area-inset-top)`, paper@88% + blur, zIndex 40 under CourseSearch/LooperSheet) — a direct port of the app's existing `.app-header` scrim; covers all four tee-time phases, invisible on desktop.
  2. **Nearby list no longer "reads as broken"** — `CourseRow` restructured to aligned `checkbox | name (+ optional city sub-line) | right-aligned distance` columns (was a ragged `distance · city` right column), `minHeight: 44` rows, consistent dashed dividers, even group-label rhythm.
  3. **Labels: never "USA"** — guarded the `r.city` fallback in `toCourseOptions` with the same country-segment regex (9f0577e covered `muniFromAddress`); a country-only city → honestly omitted (+2 unit tests). The honest no-fake-data side-effect: a country-only-city + all-generic-name row is now skipped by `hasIdentifyingTokens`.
  4. **Folded-in same-file follow-ups** — Options route-section header now conditional ("Call to book" / "Book on their site" / "Book direct", kicker "No listed times") so it can't contradict a `book_on_site` row; distance + city added to route-entry rows to match the real-slot Sections; `minHeight: 44` on the sub-44pt tee-time rows/buttons.
  - Fable-planned (`specs/teetime-prefs-ux-polish-plan.md`). **Reviewer SHIP** — options.ts/state-machine/`pick()` untouched, scrim z-order + `pointerEvents:none` correct, city guard correct, tests added not weakened. **QA PASS** — lint/tsc/build clean, voice smoke 274/274, full vitest 1882/1882 (incl. 74/74 targeted teetime). **Designer PASS** — ran live with mocked network/real component render; Items 2/3/4 confirmed calm/on-Northstar (mixed call+book_on_site route → "Book direct" verified). Item 1 scrim approach sound (reuses `.app-header`); true iOS-standalone on-device confirmation deferred to the owner's TestFlight build.
  - Deferred (non-blocking): raw route-row `slot.city` isn't run through the country regex (new surface, same honesty class — only bites if the backend returns "USA" as a slot city); unify the two distance placements on the Options screen (real-slot kicker vs route-row inline); `filterToSelection` Set falsy-guard.

- [x] **Caddie latency telemetry now survives iOS backgrounding** (2d4b4c9) — *silent*. Per-turn caddie stage-timing events (`caddie.eos_to_transcript`, `caddie.transcript_to_first_token`, `first_token_to_first_audio`, headline `caddie.eos_to_first_audio`; surfaces `caddie-turn` vs `caddie-rt`) were dying on the owner's iPhone (Capacitor WKWebView) — prod saw only ~1 headline event in 3 days, blocking the cascaded-STT go/no-go (#126). Root cause (Fable-diagnosed, correcting the backlog's premise): the headline ALREADY flushed immediately at `markFirstAudio()` (shipped 6fcb40d, live the whole window), but the EARLIER legs only rode that single flush — and on iOS `markFirstAudio()` (via `useSheetTTS` `onSpeakStart`) often never fires before the webview suspends, so the whole turn's timing was lost. Fix = two guarded `safeFlush()` calls in `markTranscript()`/`markFirstToken()` (`frontend/src/lib/voice/caddie-turn-timing.ts`) so each leg reaches the backend the moment it's computed; `markFirstAudio()` keeps its single terminal flush (both audio legs + the RT headline). Guards/idempotence/clamps untouched; only these caddie legs opt into immediate flush (≤3 POSTs/classic turn, human-paced — non-timing telemetry still batches). **Reviewer SHIP** — flush guarded inside `if (ms !== null)` (clamped legs never POST), PII-safe (`{ ms }` only, pinned by test), throw-isolated, empty-queue flush no-ops; tests strengthened not gamed (new "incomplete turn still ships `eos_to_transcript`" tooth). **QA PASS** — all 6 gates green (lint/tsc/build, voice 274/274, targeted vitest 34/34, ruff). Effect: once the owner uses the live caddie a few times on the next TestFlight build, prod gets real `caddie-turn` vs `caddie-rt` p90 → the cascaded-STT decision (#126) becomes readable.

- [x] **The caddie greets you — it no longer puts words in your mouth** (36cbe5f + 1446ea2) — *noticeable*. From the owner's 2026-07-09 screenshots: when the caddie sheet auto-opened it fabricated a first-person question AS the player ("I'm on the tee, about 231 yards to the pin. What should I hit off the tee?") and rendered it as the player's own bubble + sent it as a user turn. Now the CADDIE opens the conversation itself with a calm, yardage-book-voiced greeting — assistant-authored, never a fake player utterance. New copy: tee → "You're on the tee — about 231 to the pin. Want a read on the tee shot?"; else → "About 150 to the pin from here. Want a read on the shot?". Single shared builder (`buildOpeningGreetingText`) feeds both paths: the classic sheet seeds it directly as a `{role:"assistant"}` turn + TTS (no network round-trip, no user bubble/history), and live mode instructs the Realtime model to voice it via a new `sendOpener` (system-role item + `response.create`, no local user bubble) — so the opener always renders as the caddie, and the mic auto-arms to invite a reply. Fable-planned (`specs/caddie-remove-seeded-question-plan.md`; authorship/role decision was the crux, not the copy). **Reviewer SHIP** — all 7 checks confirmed from the diff: no user-role opener artifact on either path, single-source-of-truth preserved, connect ordering (silent hole re-anchor before opener) intact, all double-fire/honest-idle guards byte-identical, tests re-pointed not gamed (the core lock got *stronger*: `onUpdateConvHistory` asserted to receive exactly `[{role:"assistant"}]` with all network mocks uncalled). **Designer APPROVE** — calm/on-Northstar, dropping "yards" is authentically caddie-voiced, em-dash reads fine under TTS, reuses the existing caddie-bubble component (no invented affordance). **QA** — lint/tsc clean, targeted vitest 79/79, full vitest 1890/1890, voice smoke 274/274. Frontend-only; zero schema/backend change.

- [x] **Tee-time results/confirmation never show a bare country** (1d2d0ae) — *noticeable*. Closes the deferred honesty follow-up flagged on the polish item above: the prefs course list already dropped a country-only "USA" label, but the RESULTS ("Take your pick"), route-entry rows, and the booking CONFIRMATION stamp still rendered `g.city` / `slot.city` verbatim — and those come from the backend `slot.city = raw course.address` (foreup.py / routing.py), so a booked "Marine Park Golf Course" could read "USA · 18 holes". Normalized all three backend-fed render sites in `frontend/src/app/tee-time/page.tsx` through the existing tested `muniFromAddress`: it drops country segments ("USA"/"United States"), extracts a real locality from a full address ("…,Brooklyn,NY 11234,USA"→"Brooklyn"), and passes a clean city+state through ("San Francisco, CA"→"San Francisco"). The confirmation stamp guards the separator (`{cityLabel ? \`${cityLabel} · \` : ""}{holes} holes`) so an omitted locality never leaves a dangling "· ". Frontend-only; backend address source unchanged. **Reviewer SHIP** — no dangling separator, no undefined crash, no over-strip of a real US city, render-only (ICS calendar `city` correctly left on the fuller raw value; grouping is by courseId), no tests weakened. **Designer APPROVE** — omission-over-fabrication matches the no-fake-data / on-paper restraint, no orphan separators or SaaS drift. **QA** — lint/tsc clean, voice smoke 274/274, targeted vitest 74/74. The header "WHERE / N SELECTED" copy was left intentionally (consistent cross-Section idiom; "4 SE…" is the aside truncating "selected" — a shared-component layout pass, not a contained win). Follow-up (non-blocking): `muniFromAddress` returns the last surviving address segment, so an address with no real city could surface a pseudo-locality — worth a "last-segment-wins" lock test on the shared helper (pre-existing, out of scope here).

- [x] **Yardage card never fabricates Front/Center/Back** (071e2d4 + 31c2067) — *noticeable (correctness / no-fake-data)*. On a round with a course-center anchor (`round.courseLat/Lng` → `roundAnchor` truthy) but no mapped hole geometry, the F/C/B tiles rendered fabricated `distance ± offset` numbers (`distance` = `max(80, hole.yards*0.4)`, a ~40% ILLUSTRATION placeholder) that silently disagreed with the scorecard — for every hole, persistently, plus a load-window flash on mapped rounds. Extracted pure `buildFcbTiles` + `effectiveFcbSource` (`frontend/src/lib/course/fcb-tiles.ts`, unit-tested): every "no real geometry" state (`fcbSource==='card' || fcb==null`) now collapses to the honest card-only tiles (Front/Back "—", Center = scorecard yardage), and the caption derives from the SAME condition so it reads "from the card", never "from the tee" over "—" tiles. The three working paths (GPS "you", from-tee, existing card-only) are byte-identical. **Reviewer SHIP** — fabricated number gone on every path, three paths traced byte-identical, `fcb!` non-null access provably safe (TS aliased-const narrowing), reachability confirmed as a real persistent bug, tests genuine (regression guard: no numeric front/back leaks). **Designer APPROVE** — "—" presentation identical to the existing card-only look (matches Wind/Elev "—" idiom), calm/on-Northstar; caught + folded the caption-honesty fix. **QA** — lint/tsc clean, voice 274/274, vitest 39/39 targeted (new fcb-tiles + fcb-labels + DistancesCard). Frontend-only; zero schema/backend change. Follow-up (tracked `fcb-plays-tile-fabricated-fallback`): the PLAYS tile still derives from `distance` in the same fallback state.

**Six noticeable items + one silent** so far; more may accumulate before the next "ship it". Required CI gates will be re-verified strict-green — every REQUIRED gate state:SUCCESS on the FINAL head SHA — at ship time.

🤖 Generated with [Claude Code](https://claude.com/claude-code)

https://claude.ai/code/session_012CgufkmbGRw1MXGKEmTpei






